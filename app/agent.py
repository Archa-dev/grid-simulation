import random

class Agent:
    def __init__(self, id, x, y):
        self.id = id
        self.x = x
        self.y = y

    def step(self, grid):
        # TODO: Decide where to move (e.g., randomly)
        # Use grid.is_occupied() and grid.move_agent()
        pass
